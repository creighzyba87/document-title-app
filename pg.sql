/ (root)
├─ frontend/            # React app (single page)
│  ├─ package.json
│  └─ src/
│     └─ App.jsx
├─ backend/
│  ├─ package.json
│  └─ index.js          # Express server: /upload endpoint
├─ scripts/
│  └─ extract_text.js   # text-extraction helper (used by server)
├─ README.md
└─ .render.yaml         # Render service config (optional)
